import styled from "@emotion/styled";

export const GameTitle = styled.div({
  fontSize: "3.5rem",
  textAlign: "center",
  lineHeight: "1.2",
  margin: "50px 0",
});

export const GameOverTitle = styled.div({
  fontSize: "2.5rem",
  textAlign: "center",
  marginBottom: "20px",
  lineHeight: "1.2",
});

export const RestartBtn = styled.span({
  fontSize: "1.5rem",
  display: "inline-block",
  padding: "0.6rem 1rem",
  cursor: "pointer",
  color: "#fff",
  background: "#551a8b",
});
